[pytest]
minversion = 7.0
testpaths = tests src
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings
pythonpath = src
markers =
    unit: unit tests
    integration: integration tests
    slow: slow running tests
