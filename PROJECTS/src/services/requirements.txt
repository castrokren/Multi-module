# Crawler pipeline dependencies (all stages + watcher + GUI)
# Versions = known-good set from the dev machine (2026-07-09)
pandas>=2.2
openpyxl>=3.1
xlrd>=2.0
requests>=2.32
urllib3>=2.4
beautifulsoup4>=4.14
pdfplumber>=0.11
PyPDF2>=3.0
watchdog>=6.0
psutil>=7.0
chardet>=5.2

# Build-only (packaging the GUI exe)
# pyinstaller>=6.20
