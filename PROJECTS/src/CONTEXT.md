## Source Context

`src/` contains implementation code for the workspace:

- `services/`: core service modules (`classify`, `cross-reference`, `scraper-full`) and pipeline orchestration.
- `components/`: reusable building blocks for future UI/app composition.
- `utils/`: shared helper assets and support utilities.
- `tests/`: test code and validation harnesses.
