## Operations Context

`ops/` contains runtime and operational assets:

- `deploy/`: deployment scripts and environment setup.
- `monitoring/`: watcher scripts, pipeline logs, and monitoring configs.
- `scripts/`: support scripts and local runtime artifacts (including `venv`).
